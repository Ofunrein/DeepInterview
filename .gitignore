# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
.next/
# Netlify CLI build output (functions, edge handlers, vendored deno std).
.netlify/
# Netlify CLI writes this when bundling edge functions.
deno.lock
.turbo/
out/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Enterprise (commercial license) — kept out of the OSS repo.
# Exception: packages/ee is the open-core seam STUB (inert defaults,
# Apache-2.0) — tracked in OSS; downstream distributions replace its contents.
ee/
!packages/ee/

# Product spec / internal design docs (kept local, not published)
site/

# Local agent instructions — never publish
CLAUDE.md


# Avatar media is never committed (docs/AVATARS.md): local-dev drop-in only;
# accepted packs are published as GitHub Release artifacts + CDN.
apps/web/public/avatars/
